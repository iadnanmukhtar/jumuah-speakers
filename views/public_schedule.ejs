<%
  const hasAny = (upcomingSlots && upcomingSlots.length) || (remaining && remaining.length);
%>

<section class="space-y-6">
  <div class="bg-gradient-to-r from-emerald-700 via-emerald-600 to-emerald-500 rounded-2xl text-white shadow-md border border-emerald-100">
    <div class="p-6 sm:p-8 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
      <div>
        <p class="text-xs uppercase tracking-wide text-emerald-100">Public schedule</p>
        <h1 class="text-2xl sm:text-3xl font-semibold">Upcoming Jumuahs</h1>
        <p class="text-sm text-emerald-50 mt-1">
          Snapshot of the next three Fridays at Masjid al-Husna
        </p>
      </div>
      <div class="flex flex-col items-start sm:items-end gap-1 sm:gap-2">
        <a
          href="/register"
          class="inline-flex items-center justify-center text-xs px-3 py-1.5 rounded-full border border-white/50 text-white hover:bg-white/10">
          Become a speaker
        </a>
        <div class="text-[11px] uppercase tracking-wide text-emerald-100">Last updated</div>
        <div class="text-sm text-white font-semibold">
          <%= lastUpdated ? helpers.formatDateLong(lastUpdated) : 'No speaker updates yet' %>
        </div>
      </div>
    </div>

    <%- include('partials/scheduleHero', { upcomingSlots }) %>
  </div>

  <div class="bg-white rounded-xl shadow-sm border border-emerald-100 p-5">
    <% if (!hasAny) { %>
      <div class="text-sm text-slate-600">
        No Jumuah entries found in this window. Please check back soon.
      </div>
    <% } else if (!remaining || remaining.length === 0) { %>
      <div class="text-sm text-slate-600">
        No additional slots in the next two weeks beyond the upcoming Jumuah.
      </div>
    <% } else { %>
      <%- include('partials/scheduleCards', { items: remaining }) %>
      <%- include('partials/scheduleTable', { items: remaining }) %>
    <% } %>
  </div>
</section>
